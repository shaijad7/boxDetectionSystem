# Placeholder weights directory
# Place your trained YOLOv8 model weights file here as:
#   box_detection_model.pt
#
# If no custom weights exist, the backend falls back to the pretrained YOLOv8n model.
# To train a custom model:
#   1. Prepare a labeled dataset of box images (YOLO format)
#   2. Run:  yolo train data=your_data.yaml model=yolov8n.pt epochs=100
#   3. Copy the resulting best.pt here and rename to box_detection_model.pt
